import 'package:flutter/cupertino.dart';

class NoStateProvidedScreen extends StatelessWidget {
  const NoStateProvidedScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('no state provided'));
  }
}
